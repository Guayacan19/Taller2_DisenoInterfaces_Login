<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Taller2_fabian">

        <!-- SPLASH  -->
        <activity
            android:name=".iu.inicio2.SplashActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- BIENVENIDO -->
        <activity
            android:name=".iu.inicio2.BienvenidoActivity"
            android:exported="false" />

        <!-- LOGIN -->
        <activity
            android:name=".iu.autch.LoginActivity"
            android:exported="false" />

        <!-- REGISTRO -->
        <activity
            android:name=".iu.autch.RegistroActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".iu.main.MainActivity"
            android:exported="false" />

    </application>

</manifest>